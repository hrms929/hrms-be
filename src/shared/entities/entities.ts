import { User, } from './user.entity';
import { UserProfile, } from './user-profile.entity';
import { UserToken, } from './user-token.entity';

export const Entities = [User, UserProfile, UserToken,];
